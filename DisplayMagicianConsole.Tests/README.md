# DisplayMagicianConsole.Tests

Regression tests for the DisplayMagician console application's command handling and process exit codes.

Run the tests from the repository root:

```powershell
dotnet test .\DisplayMagicianConsole.Tests\DisplayMagicianConsole.Tests.csproj
```
