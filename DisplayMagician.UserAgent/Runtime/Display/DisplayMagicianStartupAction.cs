namespace DisplayMagician.UserAgent.Runtime
{
    public enum DisplayMagicianStartupAction
    {
        RunShortcut,
        ChangeProfile,
        CreateProfile,
        CurrentProfile,
        StartUpNormally,
        AllProfiles
    }
}